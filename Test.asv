x = [0:0.0001:4];
y = (1./x) .* (1 - erf(1./sqrt(x)));
plot(x,y);
h_mean = 16;
increment = 0.1;
max_std_dev = 25;
std_dev = [0:increment:max_std_dev];
%error_rate = zeros(max_std_dev/increment);

max_diff = 50;
difficulty = [0: increment: max_diff];
error_rate = zeros(max_diff/increment);
for diff_i = 1:size(difficulty)
    %error_rate(diff_i,:) = normcdf([-inf, 0], h_mean, std_dev[diff_i]));
    hi  = (normcdf([-inf, 0], difficulty(diff_i), h_mean));
    error_rate(diff_i,:) = hi(2);
    disp(error_rate(diff_i,:));
end
disp(size(difficulty
plot(difficulty, error_rate);
disp(error_rate);
%error_rate = int(h_curve, x, -inf, 0)